import 'package:flutter/cupertino.dart';

class HospitalLocations extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('hospital locations'));
  }
}
